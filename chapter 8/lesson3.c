//convnient way to print string
#include <stdio.h>

int main() {
    char *ptr = "Varun"; //it is converted into lesson1 's form varun is a char array so directly written pointer
    printf("%s", ptr);
    return 0;
}